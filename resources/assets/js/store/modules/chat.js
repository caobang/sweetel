import {api} from '../../api'

// 应用初始状态
const state = {
    
}

// 定义getters
const getters = {
    
}

// 定义mutations
const mutations = {
    
    
}

// 定义actions
const actions = {
    
}

export default {
    state,
    getters,
    mutations,
    actions
}